# role-manager Development Guidelines

Auto-generated from all feature plans. Last updated: 2025-11-26

## Active Technologies
- TypeScript 5.x, React 18 + `@tanstack/react-query`, `@openzeppelin/ui-builder-*` packages, `react-router-dom` (007-dashboard-real-data)
- IndexedDB via `@openzeppelin/ui-builder-storage` (existing) (007-dashboard-real-data)
- TypeScript 5.x, React 18.x + `@tanstack/react-query`, `@openzeppelin/ui-builder-*` packages, `lucide-react` (009-roles-page-data)
- IndexedDB via `@openzeppelin/ui-builder-storage` (Dexie) (009-roles-page-data)

- TypeScript 5.x (via existing monorepo config) (003-data-store-service)
- IndexedDB (via Dexie) (003-data-store-service)
- TypeScript 5.9, React 19 + `@openzeppelin/ui-builder-ui`, `@openzeppelin/ui-builder-storage`, `@openzeppelin/ui-builder-types`, `react-hook-form` (004-add-contract-record)
- IndexedDB via Dexie (`@openzeppelin/ui-builder-storage`) - existing `RecentContractsStorage` (004-add-contract-record)
- TypeScript 5.x with React 19, extends `RecentContractsStorage` with schema fields (005-contract-schema-storage)

- TypeScript 5.x (latest stable matching UI Builder) (001-role-manager-setup)

## Project Structure

```text
src/
tests/
```

## Commands

npm test && npm run lint

## Code Style

TypeScript 5.x (latest stable matching UI Builder): Follow standard conventions

## Recent Changes
- 009-roles-page-data: Added TypeScript 5.x, React 18.x + `@tanstack/react-query`, `@openzeppelin/ui-builder-*` packages, `lucide-react`
- 007-dashboard-real-data: Added TypeScript 5.x, React 18 + `@tanstack/react-query`, `@openzeppelin/ui-builder-*` packages, `react-router-dom`

- 005-contract-schema-storage: Added schema loading via adapters, extends existing `RecentContractsStorage`

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
